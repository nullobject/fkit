/**
 * Returns the value `a` unchanged.
 *
 * @summary The identity function.
 *
 * @example
 *
 * F.id(1) // 1
 *
 * @param a A value.
 * @returns The value `a`.
 */
export default function id (a) {
  return a
}
